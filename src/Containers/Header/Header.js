import React, { Component } from 'react'
import  './Header.css';

export default class Header extends Component {
    render() {
        return (
            <div id="header">
                Custom - Tokens <span className='small' > (Ascii - Samples)</span> 
            </div>
        )
    }
}
